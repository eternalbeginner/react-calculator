import styled from 'styled-components';

export const StyledSaved = styled.span`
  display: flex;
  flex-direction: row;
  align-items: baseline;
  justify-content: end;
  gap: 5px;
  width: 100%;
  height: fit-content;
  break-after: always;
  color: rgba(0, 0, 0, 0.5);
  font-size: 0.9rem;
  text-align: right;
  word-break: break-all;
`;
